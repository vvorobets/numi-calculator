const generalVariablesList = [
    // time
    'time', 'now', 'today', // ! +'in'
    // constants
    'Pi', 'E', 
    // CSS variable
    'ppi',
    // as variable
    'prev', 

];

const customVariablesList = [];